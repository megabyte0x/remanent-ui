export enum TransactionType {
    MINT,
}

export enum NFTStatus {
    ADDED,
    LISTED_FOR_BID,
    MINTED,
}

export enum Network {
    ETHEREUM = 1,
    POLYGON = 137,
}

export enum View {
    NFTs,
    DAOs,
}

export enum NFT_API {
    NFTs,
    Collections,
    Transactions,
}

export enum DAO_API {}
